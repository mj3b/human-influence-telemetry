# Human Influence Telemetry v0.4.0 Rubric Stabilization Plan

**Status:** Active development plan  
**Branch:** `develop/hit-v0.4.0-rubric-stabilization`  
**Governing decision:** `ADR-0002`  
**Candidate specification:** `specification/HIT-SPECIFICATION-v0.4.0-candidate.md`  
**Candidate handbook:** `docs/application-handbook-v0.4.0-candidate.md`  
**Research maturity:** Level 1, Defined  
**Human reliability claim:** Unresolved

## 1. Release objective

Version `0.4.0` will convert the 16 ambiguity classes recorded in `HIT-ARFR-001` into a synchronized, testable assessment contract.

The release is a normative-method update. It will not establish human inter-rater reliability, field effectiveness, legal compliance, certification, or independent adoption.

## 2. Approved contract changes

ADR-0002 approves:

- an evidence-state model separating affirmative absence, formal presence, operational capability, and observed exercise;
- explicit thresholds for `0`, `1`, `2`, and `IE`;
- dimension-specific rules for Counsel, Judgment, Command, Correction, Repair, and Reform;
- Repair trigger states;
- separate institutional-record and assessment-packet integrity statuses;
- deterministic derivation of the overall Telemetry Integrity status;
- a dominant-pattern default for period-level aggregation;
- a required actor-authority matrix;
- source-conflict weighting rules;
- construct-specific cross-dimensional evidence reuse;
- precise citation locators and bounded evidence propositions.

## 3. Work-package status

| Work package | Status | Primary artifacts |
|---|---|---|
| WP-A: Normative specification | Complete for schema design | candidate specification, approved rule trace, ADR-0002 |
| WP-B: Practitioner procedure | Complete for schema design | candidate application handbook |
| WP-C: Public data contract | Next | assessment schema and dimension catalog |
| WP-D: Executable fixtures | Pending WP-C | accepted, rejected, and boundary fixtures |
| WP-E: Case migration | Pending WP-C and WP-D | separate `0.4.0` case records and migration report |
| WP-F: Validation and release controls | Pending | validator, CI, migration report, release metadata |

WP-A and WP-B are fixed design targets. Machine representation may expose a contradiction or impossible requirement; such a defect must return to normative review rather than be silently resolved in code.

## 4. WP-C: Public data contract

Files:

- `schema/hit-assessment.schema.json`;
- `schema/hit-dimension-catalog.json`;
- proposed candidate schema and catalog files until migration is complete.

Required additions:

- `aggregation_rule` and assessment scope;
- actor-authority matrix;
- structured evidence claims with locators and proposition relations;
- evidence-state rationale for each substantive dimension;
- Repair trigger state;
- separate institutional-record and assessment-packet integrity objects;
- derived overall Telemetry Integrity status;
- explicit unresolved proposition and search record for `IE`;
- version, predecessor, and migration declarations.

Completion test: the candidate schema rejects records that omit required normative information and preserves the six-plus-one construct model.

## 5. WP-D: Executable fixtures

Required fixture classes:

1. explicit absence versus silence;
2. formal role without process applicability;
3. formal presence with no operational capability;
4. tested operational capability without observed exercise;
5. actual Counsel access versus general permission;
6. Judgment based on direct reasons versus indirect indicators;
7. Command held by a vendor rather than the assessed actor;
8. Correction intake without substantive reconsideration;
9. triggered, not-triggered, and indeterminate Repair states;
10. later Reform with and without documented linkage;
11. adequate, limited, unreliable, and `IE` integrity components;
12. rare exercise versus dominant pattern;
13. separated versus improperly combined actors;
14. resolvable versus material contradictory evidence;
15. valid versus invalid cross-dimensional evidence reuse;
16. precise versus non-reproducible citation locators.

Each class requires at least one accepted and one rejected or differently classified fixture.

Completion test: expected outcomes are deterministic and the validator rejects every known invalid state.

## 6. WP-E: Case migration

Requirements:

1. preserve all released `0.1.0` assessments as historical artifacts;
2. produce candidate `0.4.0` migrations separately;
3. explain every changed finding, evidence state, actor boundary, citation, and integrity status;
4. do not silently overwrite historical findings;
5. identify cases that cannot migrate without additional evidence.

Completion test: every public case either validates under `0.4.0` or has an explicit migration exception.

## 7. WP-F: Validation and release controls

Requirements:

- update validator constants only after specification, handbook, schema, catalog, and fixtures are synchronized;
- validate new required artifacts and fields;
- add negative tests for every friction class;
- preserve the locked human protocol and frozen packet unchanged;
- add the adjacent-system terminology audit;
- publish migration and breaking-change reports;
- synchronize changelog, citation, provenance, and release metadata;
- validate the exact release commit.

Completion test: one clean command validates the complete `0.4.0` release candidate.

## 8. Friction-to-contract matrix

| Friction | Approved rule | Schema consequence | Minimum fixture |
|---|---|---|---|
| `FR-01` | affirmative absence | unresolved proposition for `IE` | silence versus explicit nonexistence |
| `FR-02` | process-specific formal presence | applicability evidence | generic versus applicable role |
| `FR-03` | operational capability and observed exercise | evidence-state rationale | formal, tested, exercised cases |
| `FR-04` | actual pre-decision Counsel access | access event and timing | permission versus retrieval |
| `FR-05` | direct and indirect Judgment evidence | evidence type | indicators versus reasons |
| `FR-06` | practical Command control | authority and mechanism owner | institution versus vendor control |
| `FR-07` | Correction state decomposition | channel and effect fields | intake versus reconsideration |
| `FR-08` | Repair trigger states | trigger object | triggered, not triggered, indeterminate |
| `FR-09` | Reform linkage and follow-up period | change linkage | linked versus unrelated later change |
| `FR-10` | two integrity objects | two component objects | packet versus institutional record |
| `FR-11` | integrity thresholds | deterministic status enum | all four statuses |
| `FR-12` | dominant-pattern default | aggregation declaration | rare exception versus dominant pattern |
| `FR-13` | actor-authority matrix | actor relationship objects | separated versus collapsed actors |
| `FR-14` | conflict weighting | contradiction record | resolvable versus material conflict |
| `FR-15` | proposition-specific reuse | per-use evidence claims | valid reuse versus construct collapse |
| `FR-16` | locator and proposition precision | structured citation object | exact locator versus source-only citation |

## 9. Protected artifacts

This workstream must not change:

- original scorer submissions, when collected;
- protocol `HIT-IRP-CIGNA-001`;
- frozen Cigna packet contents and hashes;
- pre-adjudication threshold;
- critical-disagreement definitions;
- historical public assessments released under specification `0.1.0`;
- the statement that H3 and Maturity Level 2 remain unresolved.

## 10. Stop conditions

Do not publish `v0.4.0` when any of the following remains true:

- a friction class lacks a governing rule;
- the candidate specification and handbook conflict;
- the schema cannot represent a required rule;
- a boundary fixture has no deterministic expected outcome;
- historical and migrated case records are not clearly separated;
- the validator accepts a known invalid state;
- release language implies human reliability, legal conformity, certification, or independent adoption;
- the locked protocol or historical findings are altered without a separate decision.

## 11. Sequence

1. ~~Approve normative rules.~~ Complete.
2. ~~Create synchronized candidate specification and handbook.~~ Complete.
3. Design candidate schema and dimension catalog.
4. Add boundary fixtures and validator rules.
5. Migrate public cases separately.
6. Publish breaking-change and migration reports.
7. Run clean validation.
8. Prepare the `v0.4.0` release candidate.
