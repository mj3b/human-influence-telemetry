# Manual scorer originals

Upload the three binary files named in `../manual-originals-manifest.json` to this directory without editing, conversion, or resaving. Remove this instruction only after the files are present and their SHA-256 digests match the manifest.
